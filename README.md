# go-hello
A simple hello world application in Go.

## Run
```bash
go run .
```

By default, the app listens on `:8080` and renders:
- Hello message
- Application version
- Authors list
- A random (safe) joke with fallback text
